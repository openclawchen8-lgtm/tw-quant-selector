services:
  app:
    build: .
    environment:
      - FINMIND_TOKEN=${FINMIND_TOKEN}
      - DUCKDB_PATH=/data/tw_quant.duckdb
    volumes:
      - ./data:/data
      - ./output:/output
    ports:
      - "8000:8000"
    restart: unless-stopped
